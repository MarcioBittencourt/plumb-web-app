type Props = {}

type Cycles = {
    description: string;
    dateInit: Date;
    dateEnd: Date;
    manager: string;
    employees: [];
    goals: any[]; // tipo de avaliação, 
};

const Cycle = (props: Props) => {
    return (
        <>
        </>
    );
}
export default Cycle;